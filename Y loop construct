#include <stdio.h>

int main() {
    int i = 0;

    // While loop that runs as long as i is less than 5
    while (i < 5) {
        printf("%d\n", i); // Print value of i
        i++; // Increment i
    }

    return 0;
}
